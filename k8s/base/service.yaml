apiVersion: v1
kind: Service
metadata:
  name: ml-pipeline-api
  namespace: ml-pipeline
  labels:
    app: ml-pipeline
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ml-pipeline
    component: api
